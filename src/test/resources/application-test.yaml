spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:persondbtest
    userName: sa
    password: password
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
